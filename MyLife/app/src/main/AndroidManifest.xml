<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.im.mylife">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ExpenceAddActivity"></activity>
        <activity android:name=".TaskAddActivity" />
        <activity android:name=".NoteAddActivity" />
        <activity
            android:name=".ExpencesActivity"
            android:label="@string/title_activity_expences"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TasksActivity"
            android:label="@string/title_activity_tasks"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NotesAcivity"
            android:label="@string/title_activity_notes_acivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ProfileActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>